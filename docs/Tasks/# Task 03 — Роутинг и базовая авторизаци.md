# Task 03 — Роутинг и базовая авторизация через backend

## Цель

Настроить роутинг в Mini App и заготовку авторизации: отправка `initData` на backend.

---

## Что попросить сделать Cursor AI

> В `frontend/` настрой React Router и страницы:
> 
> - `/` — `AuthLoadingPage`
> - `/register` — `RegisterDoctorPage`
> - `/home` — `HomePage`
> 
> Логика:
> 
> 1. `AuthLoadingPage`:
>    - Использует `useTelegramInitData`.
>    - Берёт `initDataRaw`.
>    - Делает `POST` запрос на backend `/auth/telegram` (пока просто на `http://localhost:8000/auth/telegram`).
>    - Если backend вернул "doctorExists: true" + токен:
>      - Сохраняет токен в `localStorage` (или контексте).
>      - Редирект на `/home`.
>    - Если "doctorExists: false":
>      - Редирект на `/register`.
> 
> 2. `RegisterDoctorPage`:
>    - Форма: имя, фамилия, специализация, телефон, название клиники.
>    - По submit — `POST /doctors/register` на backend с токеном/или с initData (пока можно заглушку).
>    - После успешной регистрации — редирект на `/home`.
> 
> 3. `HomePage`:
>    - Показывает кнопки меню:
>      - «📋 Իմ պացիենտները»
>      - «➕ Ավելացնել նոր պացիենտ»
>      - «💳 Բաժանորդագրություն»
>      - «ℹ️ Օգնություն»
>      - «🔒 Գաղտնիության քաղաքականություն»
>    - Пока роуты по ним можно не реализовывать, но сделай заглушки.
> 
> Сделай простый API-клиент (Axios или fetch) в `src/api/client.ts`, где базовый URL вынесен в env-переменную.

---

## Что сделать самому

1. В `frontend/.env` добавить что-то вроде:
   ```env
   VITE_API_BASE_URL=http://localhost:8000
