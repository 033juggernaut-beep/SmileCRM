# Task 05 — Страницы подписки, помощи и политики конфиденциальности

## Цель

Сделать статические (пока) страницы:

- Подписка
- Помощь
- Политика конфиденциальности

---

## Что попросить сделать Cursor AI

> В `frontend/` добавь следующие страницы:
> 
> 1. `SubscriptionPage` (`/subscription`):
>    - Карточка с:
>      - Статус подписки (пока текст `Trial` / `Active` как заглушка).
>      - Текст:
>        - "7 օր անվճար փորձաշրջան, այնուհետև ամսական վճար" (армянский текст можно вставить как строку).
>      - Кнопки:
>        - "Վճարել Idram-ով"
>        - "Վճարել IDBank Pay-ով"
>      - Пока по нажатию просто показывай alert("Платеж пока не настроен").
> 
> 2. `HelpPage` (`/help`):
>    - Несколько FAQ блоков:
>      - Как зарегистрироваться
>      - Как добавить пациента
>      - Что такое триал
> 
> 3. `PrivacyPolicyPage` (`/privacy`):
>    - Заглушка политики конфиденциальности (структурированный текст, который позже можно заменить).
> 
> 4. Обнови `HomePage`, чтобы кнопки:
>    - «💳 Բաժանորդագրություն» → `/subscription`
>    - «ℹ️ Օգնություն» → `/help`
>    - «🔒 Գաղտնիության քաղաքականություն» → `/privacy`

---

## Что сделать самому

1. Посмотри, удобно ли читаются страницы с телефона (сделай DevTools → mobile).
2. При необходимости попроси Cursor поправить отступы и размеры шрифтов.

---

## Критерии готовности

- Все три страницы доступны из `HomePage`.
- Нет "битых" роутов.
